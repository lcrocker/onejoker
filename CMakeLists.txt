# Top-level CMakeLists.txt for OneJoker project.
#

cmake_minimum_required(VERSION 2.6)
enable_testing()
project(OneJoker)
set(PACKAGE_VERSION "0.1")

add_subdirectory(src)
# add_subdirectory(python)
